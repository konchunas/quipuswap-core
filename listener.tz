{ parameter unit ;
  storage address ;
  code { CDR ;
         DUP ;
         CONTRACT (or (unit %read) (address %write)) ;
         IF_NONE { PUSH string "cant-get-writer" ; FAILWITH } {} ;
         PUSH mutez 0 ;
         PUSH unit Unit ;
         LEFT address ;
         TRANSFER_TOKENS ;
         SWAP ;
         NIL operation ;
         DIG 2 ;
         CONS ;
         PAIR } }

